# INFORME BOTONES INICIO CORREGIDOS

## Corrección
- `showCreate` abre `createView`.
- `showJoin` abre `joinView`.
- Añadida navegación robusta por `data-target-view`.
- Añadido enlace en `DOMContentLoaded` y llamada de seguridad.
- Botones reforzados con `z-index` y `pointer-events`.

## Validación
JavaScript: OK
Firebase Rules JSON: OK
Manifest JSON: OK
